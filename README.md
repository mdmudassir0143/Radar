# Radar

Trace an Algorand payTo address to see who sent USDC, who received it, and how those wallets connect.

```bash
npm install
npm run dev
```

Open http://localhost:5173/, paste a 58-character payTo, and press Trace.
